#!/usr/bin/env bash
# Launcher for MyApp (wire fouad)
# Resolves to the directory this script lives in so it runs from anywhere
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
exec python3 "$SCRIPT_DIR/fouad_wire.py" "$@"
